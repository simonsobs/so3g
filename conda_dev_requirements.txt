# This is the set of conda packages needed when creating
# a development environment for building spt3g / so3g
# using conda compilers and packages to meet dependencies.
#
# For example, assuming you have a conda-forge base environment
# you can do:
#
#  conda create -n dev python==3.12
#  conda activate dev
#  conda install --file conda_dev_requirements.txt
#  python3 -m pip install -vv .
#
# Toolchain
compilers
cmake
ninja
make
# Compiled dependencies
libopenblas=*=*openmp*
libblas=*=*openblas
openblas=*=*openmp*
gsl
netcdf4
glog
ceres-solver=*=cpu*
pybind11
# Python dependencies
scikit-build-core
setuptools
setuptools-scm
numpy
scipy
astropy
matplotlib
ephem
pytz
pyaml
sqlalchemy
tqdm
pytest
# Soon we will include the conda package for these:
# qpoint
